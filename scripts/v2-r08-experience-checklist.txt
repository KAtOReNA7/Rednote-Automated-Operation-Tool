Rednote Studio V2-R08 精确构建体验清单
精确提交：{{EXACT_HEAD}}

启动边界：默认入口进入 V2；旧版只作为显式本地回退。系统不会登录、发布、发送评论或发送私信。
1. 双击“启动 Rednote Studio R08.cmd”，不添加任何参数；在“设置 → 构建版本”核对上述精确提交的前 8 位。
2. 依次打开总览、本周计划、内容、互动、书库、数据复盘和设置，确认 N1—N7 均可导航且无旧版页面混入。
3. 在“内容”完成一次本地版本操作，完全退出后重新默认启动，确认内容版本与状态恢复。
4. 在“互动”完成一次本地评论或私信建议操作，完全退出后重新默认启动，确认关联、建议、revision 与状态恢复。
5. 核对发布、评论和私信仍须由用户在官方平台手工完成，应用没有自动平台操作。
6. 完全退出并确认没有 RednoteMysteryOperations 或其 Electron 子进程残留。
7. 如 V2 出现阻断，再双击“启动旧版回退.cmd”；确认它明确进入旧版且不会被误认为推荐入口。

R08 默认入口与回退边界等待用户本人对本精确构建验收；PR 保持 Draft，禁止提前合并。
