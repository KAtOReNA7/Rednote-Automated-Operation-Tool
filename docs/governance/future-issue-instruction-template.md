# Future Issue 增量指令模板

> 完整遵守根级及作用域内 `AGENTS.md`；本指令只定义当前 Issue 的增量，不复制公共过程规则、
> 三层验证门禁或永久硬约束。

## 1. 用户结果与停止点

- 用户可观察结果：
- 明确停止点：
- 下一 Issue/未来能力不在本轮自动开始：

## 2. 语义基线与复用

- 必须复用的既有 Domain、Schema、repository、workflow、IPC 与 UI：
- 单一事实来源及 lineage：
- 开工时需以实际代码/测试确认的能力：

## 3. 允许范围与明确不做

- 允许修改：
- 禁止修改：
- 禁止的外部副作用、真实数据、网络、模型与费用：

## 4. 默认预算核对

引用 `AGENTS.md` 的 Issue 默认预算，不在此复制阈值。开工时填写预测与口径；需要例外时，必须
记录用户在任务开始前的明确批准。

| 维度             | 预测 | 实际 | 例外授权      |
| ---------------- | ---: | ---: | ------------- |
| 生产源码 LOC     |      |      | 无 / 授权引用 |
| 测试 LOC         |      |      | 无 / 授权引用 |
| 净新增业务表     |      |      | 无 / 授权引用 |
| 新增 trigger     |      |      | 无 / 授权引用 |
| 新增 IPC channel |      |      | 无 / 授权引用 |
| 变更文件         |      |      | 无 / 授权引用 |
| 指令行数         |      |      | 无 / 授权引用 |
| 核心验收项       |      |      | 无 / 授权引用 |

## 5. 核心验收

定义 10—20 个用户结果或关键不变量。参数排列使用表驱动测试，选择有代表性的正常、失败、
边界、安全、迁移与恢复证据，不为每个排列复制验收条目。

| ID  | 行为/不变量 | 证据计划 | 结果   |
| --- | ----------- | -------- | ------ |
| A01 |             |          | 未执行 |

## 6. 风险与验证选择

- 受影响领域与专项测试：
- 直接相邻的集成、约束或 migration：
- 独立环境信号及适用理由：
- 不适用门禁及理由：
- 预计采用的 `AGENTS.md` 门禁层级：

## 7. 交付与报告

- 必需代码、合同、ADR、migration、UI 或文档：
- 提交数量与提交信息：
- 最终需报告的动态 Git、预算、验证、外部副作用和未完成范围：

完成当前 Issue 的获准提交与报告后立即停止。
