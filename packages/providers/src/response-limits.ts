export const PROVIDER_LIMITS = Object.freeze({
  maxImageCount: 8,
  maxImageOutputBytes: 8 * 1024 * 1024,
  maxImageOutputCount: 4,
  maxImageOutputTotalBytes: 24 * 1024 * 1024,
  maxImagePromptCharacters: 20_000,
  maxInputImageBytes: 8 * 1024 * 1024,
  maxInputImageTotalBytes: 24 * 1024 * 1024,
  maxJsonArrayItems: 1_000,
  maxJsonDepth: 24,
  maxJsonNodes: 20_000,
  maxJsonStringCharacters: 200_000,
  maxMessages: 64,
  maxOutputCharacters: 1_000_000,
  maxRequestBodyBytes: 2 * 1024 * 1024,
  maxResponseBodyBytes: 8 * 1024 * 1024,
  maxResponseHeaderBytes: 32 * 1024,
  maxStopSequences: 8,
  maxTextPartCharacters: 100_000,
  maxTotalTextCharacters: 500_000,
} as const);
