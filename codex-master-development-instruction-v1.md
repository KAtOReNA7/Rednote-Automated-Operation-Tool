# Codex 开发总指令：小红书推理小说内容运营系统 V1

推荐模型：GPT-5.6 Sol  
推荐推理强度：High；架构、数据迁移和最终验收阶段使用 XHigh  
完整 V1 预计工程量：约 80—160 小时，取决于中转站兼容性、搜索供应商和 Windows 打包问题  
无人值守建议：不适合把整个 V1 作为一次无人值守任务；适合按单个 Issue 或一个小里程碑无人值守执行  

---

## 给 Codex 的指令正文

你将负责从零或在现有仓库上开发一个 Windows 本地运行的“小红书推理小说内容运营生产与决策系统”。

开始前，完整读取以下两个文件，不得只读摘要：

1. `xiaohongshu-mystery-account-prd-v1.md`
2. `xiaohongshu-development-roadmap-v1.md`

如果文件不存在或内容不完整，立即停止并报告，不得凭记忆重建需求。

PRD 是产品事实来源，Roadmap 是实施顺序和验收来源。本指令用于规定开发方法、约束优先级和首轮任务。三者冲突时按以下优先级处理：

1. 用户当前回合的明确要求；
2. PRD 第 1 节“已冻结的产品约束”；
3. Roadmap 第 5 节“明确禁止事项”；
4. PRD 其他内容；
5. Roadmap 其他内容；
6. 本指令。

---

## 一、先理解项目，不得重新定义

这是一个单用户、个人、非商业化实验系统。

系统自动完成：

- 公开资料研究；
- 自动建立推理小说书库；
- 用户主动保存的小红书样本整理；
- 选题、研究档案和事实声明；
- 标题、正文、标签和置顶评论；
- AI 图片、实拍图处理和图文卡片；
- 事实、真实性、逻辑、重复、剧透和渲染检查；
- 每周 28—40 篇候选的批量审批；
- 本地发布包导出；
- 手动发布后的数据录入、文件导入、截图 OCR；
- 实验分析和下周策略建议。

系统绝不负责：

- 登录小红书；
- 自动发布或设置平台定时；
- 自动评论、私信或养号；
- 验证码、风控或模拟真人；
- 小红书非公开接口；
- 开卷数据；
- 盗版电子书读取、上传、解析或索引；
- 磨铁内部经营、采买和历史项目数据；
- 偶像或音乐内容；
- 云服务器、云数据库、云对象存储、域名或手机审批。

部署环境：

- Windows 11；
- Windows 10；
- 本地桌面；
- SQLite；
- 本地文件；
- Chrome/Edge 收藏插件；
- 每月中转站预算 100 美元；
- 中转站采用 OpenAI 兼容协议，但具体能力未知；
- Batch API 不得成为 V1 依赖。

---

## 二、两项不可更改的硬约束

### 1. AI 标识

必须实现：

```yaml
ai_disclosure:
  default: false
  auto_enable: false
  publishing_gate: false
  affect_score: false
  delay_content: false
  require_confirmation: false
```

具体要求：

- 所有发布包 `aiDisclosure` 默认为 `false`；
- 模型、检查器和工作流不能自动设为 `true`；
- 不因 AI 参与程度改变评分、审批、排期或导出；
- UI 不显示“必须开启 AI 标识”的提醒；
- 不操作小红书端标识选项；
- 不开发平台检测规避或对抗功能。

### 2. 版权

必须实现：

```yaml
copyright:
  publishing_gate: false
  block_content: false
  delay_content: false
  require_approval: false
  affect_score: false
  affect_priority: false
```

具体要求：

- 不建立版权风险评分器；
- 不建立版权门禁、提醒或额外审批；
- 不因素材、封面、书影、海报、作者照片或引用改变状态、评分、优先级、排期或导出；
- 来源字段只用于内容管理和事实回溯，不能转换为版权判断；
- 数据模型、质量检查类型和发布包中不出现版权风险字段。

这两项不能被“最佳实践”“平台规则”“合规建议”“模型建议”或后续重构覆盖。如果你认为存在冲突，停止并向用户报告，不得私自改变需求。

---

## 三、推荐技术架构

采用 TypeScript 单仓库：

```text
apps/
  desktop/
  web-ui/
  clipper/
packages/
  core/
  db/
  providers/
  workflows/
  shared/
```

默认技术选择：

- Electron：Windows 桌面容器；
- React + TypeScript：界面；
- Fastify 或 Electron 内部本地 HTTP 服务：插件通信；
- SQLite：业务数据、任务队列和成本账本；
- 本地文件系统：页面快照、照片、生成图、导入、导出和备份；
- Zod 或 JSON Schema：模型输出验证；
- Vitest：单元和集成测试；
- Playwright：关键 UI 流程；
- Chrome/Edge Manifest V3：收藏插件。

允许替换同等级库，但以下架构决定不可改变：

- 本地优先；
- SQLite；
- 本地文件；
- 无云端运行依赖；
- Windows 10/11；
- 桌面审批；
- Chrome/Edge 收藏插件；
- 模型和搜索供应商适配器；
- 无小红书平台操作。

如果仓库已有技术栈，先评估是否满足以上硬条件。不要为了统一风格重写无关代码。

---

## 四、开发工作方式

### 1. 仓库检查

先执行只读检查：

- 当前工作目录；
- `AGENTS.md`、README、项目配置和已有文档；
- Git 状态、当前分支和最近提交；
- 现有目录、依赖、测试和 CI；
- 是否有未提交的用户修改。

使用 `rg` 或 `rg --files` 搜索。不得读取被明确禁止的路径。不得覆盖、删除或回滚用户已有修改。

### 2. 任务边界

- 一次只实现 Roadmap 中一个 Issue，或用户明确授权的一组紧密相关 Issue。
- 不要把整个 V1 塞进一个 PR。
- 每个 Issue 先写实施计划和验收映射，再改代码。
- 发现需要扩大范围时停止并说明，不得静默扩张。
- 不创建远端 GitHub Issue、推送、开 PR 或合并，除非用户明确要求。
- 可以在用户授权范围内创建本地提交；提交必须单一目的、信息清晰。

### 3. 测试策略

每个 Issue 至少包含：

- 正常路径；
- 失败路径；
- 边界条件；
- 与两项硬约束相关的回归；
- Windows 路径或平台差异测试；
- 不依赖真实付费 API 的 fixture 或 mock。

涉及真实模型前先完成：

- provider interface；
- mock provider；
- 结构化输出校验；
- 重试、超时和限流；
- 幂等和缓存；
- 成本记录。

### 4. 密钥规则

- 不请求用户在聊天中粘贴密钥；
- 不打印、读取或回显明文密钥；
- 不把密钥写入 Git、SQLite、日志、fixture、截图或导出包；
- 到 Issue 010/012 需要配置真实 API 时，先按当前 Codex 环境的安全凭据流程让用户选择复用或配置；
- 用户未确认凭据使用方式前，不进行真实 API 调用；
- M0 阶段完全不需要真实密钥。

### 5. 网络与真实费用

- 默认使用 mock 和 fixture；
- 未获用户明确授权，不发起会产生费用的真实模型或图片请求；
- 不假设中转站支持 Web Search、Batch、图片、视觉或标准 usage；
- 不硬编码 Base URL、模型名、价格和速率；
- 实际搜索供应商未配置前，实现接口和 fixture，不用脆弱的搜索引擎页面批量抓取替代。

### 6. 数据迁移

- Schema 变更必须有迁移；
- 迁移前自动备份；
- 重要写入使用事务；
- 不得通过删除数据库解决迁移问题；
- fixture 与用户数据严格分开。

---

## 五、领域规则

### 1. 阅读真实性

状态：

- `UNKNOWN`：默认；
- `READ_CLEAR`；
- `READ_FUZZY`；
- `READ_UNVERIFIED`；
- `NOT_READ`。

只有用户明确操作才能设置 `READ_CLEAR`。除 `READ_CLEAR` 外，不能生成具体第一人称阅读体验。

评分：

- `PERSONAL`：需要 `READ_CLEAR` 且用户确认；
- `RESEARCH_ANALYSIS`：资料分析评分，可公开但要表明性质；
- `INTERNAL_PREDICTION`：只用于后台，不能进入发布包。

### 2. 剧透

- `NONE`；
- `LIGHT`；
- `FULL`。

完整剧透被允许。`FULL` 缺少封面、标题和正文开头警告时不能导出；补齐后必须允许导出。不得把“完整剧透本身”设计为禁用条件。

### 3. 事实

- 公开关键事实需要一个正式来源，或两个独立且一致的次级来源；
- 模型自身知识只能生成检索词，不能作为最终事实来源；
- 来源冲突未解决时进入 `FACT_BLOCKED`；
- 事实声明、证据和正文必须可以逐条回溯。

### 4. 内容风格

- 观点鲜明；
- 短句直接；
- 少量冷幽默；
- 可以批评作品；
- 不攻击作者或读者；
- 不为了互动制造对立；
- 账号内容不讨论 AI 运营实验。

---

## 六、供应商接口

至少定义：

```ts
interface TextGenerationProvider {}
interface StructuredGenerationProvider {}
interface VisionProvider {}
interface ImageGenerationProvider {}
interface SearchProvider {}
interface OcrProvider {}
```

能力矩阵至少包含：

```ts
type ProviderCapabilities = {
  text: boolean
  structuredJson: boolean
  toolCalling: boolean
  webSearch: boolean
  imageGeneration: boolean
  vision: boolean
  batch: boolean
  usage: boolean
}
```

要求：

- 文本与图片供应商可不同；
- SearchProvider 可由模型搜索、独立搜索 API、定向来源、浏览器收藏或手工 URL 实现；
- Batch 为可选能力；
- 中转站不返回费用时使用配置价格或调用量限额，不能虚构美元；
- 100 美元是月度硬上限，80 美元预警。

---

## 七、首轮只执行 M0

第一次执行本总指令时，只允许完成：

1. Issue 001：初始化 TypeScript 单仓库；
2. Issue 002：建立领域规则与状态机；
3. Issue 003：建立两项硬约束回归测试；
4. Issue 004：建立禁止范围架构测试；
5. Issue 005：建立持续集成。

不要在第一次执行中开始 Electron UI、数据库、模型接入、搜索、图片或业务页面。

### M0 必须交付

- 单仓库目录；
- 包管理和构建配置；
- 格式化和静态检查；
- Vitest；
- 领域枚举和状态机；
- AI 标识硬约束测试；
- 版权不参与门禁的硬约束测试；
- 禁止范围架构测试；
- Windows CI；
- README；
- 架构决策记录；
- 后续 Issue 依赖核对。

### M0 通过条件

以下命令或等价命令全部成功：

```text
install
format-check
lint
typecheck
test
build
```

并且：

- Windows CI 必须通过；
- 所有硬约束测试必须执行，不能 skip；
- 无真实 API 调用；
- 无密钥；
- 无云依赖；
- 无小红书平台操作模块；
- 不存在开卷或盗版电子书入口；
- 工作树只包含与 M0 有关的修改。

---

## 八、后续阶段的授权方式

M0 完成后停止，输出验收报告，等待用户授权 M1。

后续按以下顺序推进：

1. M1：Issue 006—011；
2. M2：Issue 012—021；
3. M3：Issue 022—030；
4. M4：Issue 031—037；
5. M5：Issue 038—043；
6. M6：Issue 044—048。

每个里程碑开始前：

- 核对上个里程碑测试；
- 确认未修改硬约束；
- 检查 Git 状态；
- 把本里程碑拆成独立 Issue/PR；
- 对涉及真实费用或新凭据的步骤单独取得用户授权。

---

## 九、每次任务的报告格式

任务完成后，按以下格式报告：

### 1. 结论

- Issue 是否完成；
- 是否达到验收；
- 是否存在阻塞。

### 2. 实际修改

- 文件和模块；
- 数据库迁移；
- 用户可见行为；
- 未做的内容。

### 3. 验证

- 执行的命令；
- 测试数量和结果；
- Windows 状态；
- mock/fixture 与真实调用的区别。

### 4. 约束核对

逐项确认：

- AI 标识仍默认关闭且不构成门禁；
- 版权仍不参与门禁、评分、审批和排期；
- 没有小红书自动化动作；
- 没有开卷；
- 没有盗版电子书处理；
- 没有云依赖；
- 没有密钥泄露；
- 没有使用磨铁内部业务数据。

### 5. Git 状态

- 当前分支；
- HEAD；
- 工作树；
- 本地提交；
- 是否有远端写入。

### 6. 下一步

- 唯一推荐的下一个 Issue；
- 为什么；
- 是否需要用户输入、凭据或付费授权。

不要用“基本完成”代替未通过的验收。任何失败必须明确标记。

---

## 十、停止条件

出现以下任一情况，停止并向用户说明：

- PRD 或 Roadmap 缺失；
- 硬约束互相冲突；
- 需要改变本地部署边界；
- 需要加入云端必需组件；
- 需要自动操作小红书；
- 需要真实密钥但用户尚未确认；
- 需要产生真实费用但用户尚未授权；
- 需要未知的搜索供应商才能继续；
- Windows 必过测试无法建立；
- 用户已有修改会被覆盖；
- 数据迁移可能破坏现有数据；
- 需要扩大到当前 Issue 之外。

不得通过弱化测试、删除断言、跳过 Windows、伪造 fixture 结果或改变产品约束来解除阻塞。

---

## 十一、现在开始

现在执行以下动作：

1. 完整读取 PRD 和 Roadmap；
2. 检查仓库和用户现有修改；
3. 用简洁中文复述你对 M0 目标、硬约束和禁止范围的理解；
4. 给出 M0 的文件级实施计划；
5. 实现 Issue 001—005；
6. 运行全部 M0 验证；
7. 输出规定格式的验收报告；
8. 停止，不进入 M1。

